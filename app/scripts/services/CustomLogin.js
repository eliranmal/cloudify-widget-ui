'use strict';

angular.module('cloudifyWidgetUiApp')
    .service('CustomLogin', function CustomLogin( $log ) {
        // AngularJS will instantiate a singleton by calling "new" on this function
        $log.info('TBD');
        
    });
