'use strict';

describe('Service: TextContentCompiler', function () {

  // load the service's module
  beforeEach(module('cloudifyWidgetUiApp'));

  // instantiate service
  var TextContentCompiler;
  beforeEach(inject(function (_TextContentCompiler_) {
    TextContentCompiler = _TextContentCompiler_;
  }));
/*

  it('should do something', function () {
    expect(!!TextContentCompiler).toBe(true);
  });
*/

});
